// var preco = 2;
/*
var desconto = 0.2
const PRECO = 2

var total = PRECO - desconto */

// Const = Constante, valores imutaveis
// var = Variaveis, valores que podem sofrer alterações

function soma(a, b) {
  return a + b
}

soma(3, 5)
